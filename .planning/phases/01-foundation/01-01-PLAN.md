---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.js
  - src/storage.js
  - tests/storage.test.js
  - src/cookies.js
  - tests/cookies.test.js
autonomous: true
requirements:
  - FR-11
  - FR-12
  - NFR-1
  - NFR-5

must_haves:
  truths:
    - "saveMonth writes a JSON payload with schemaVersion:1 to localStorage keyed by 'attendance-YYYY-MM'"
    - "loadMonth returns null (not throws) for corrupt JSON, missing schemaVersion, or unavailable storage"
    - "saveMonth returns false (not throws) when localStorage is unavailable or quota exceeded"
    - "setCookie writes a cookie string containing max-age=31536000, SameSite=Lax, and Secure"
    - "getCookie retrieves the value set by setCookie for the same name"
    - "deleteCookie removes a previously set cookie"
    - "package.json has devDependencies only — no dependencies field"
  artifacts:
    - path: "src/storage.js"
      provides: "localStorage adapter with storageAvailable probe, saveMonth, loadMonth, clearMonth"
      exports: ["saveMonth", "loadMonth", "clearMonth"]
    - path: "src/cookies.js"
      provides: "setCookie, getCookie, deleteCookie helpers"
      exports: ["setCookie", "getCookie", "deleteCookie"]
    - path: "tests/storage.test.js"
      provides: "Unit tests for FR-11, NFR-5"
    - path: "tests/cookies.test.js"
      provides: "Unit tests for FR-12"
    - path: "package.json"
      provides: "Dev dependencies (vitest) and test script"
    - path: "vitest.config.js"
      provides: "Vitest config with jsdom environment"
  key_links:
    - from: "tests/storage.test.js"
      to: "src/storage.js"
      via: "import { saveMonth, loadMonth, clearMonth }"
      pattern: "import.*from.*storage"
    - from: "tests/cookies.test.js"
      to: "src/cookies.js"
      via: "import { setCookie, getCookie, deleteCookie }"
      pattern: "import.*from.*cookies"
---

<objective>
Create the test infrastructure (Vitest + config), localStorage adapter with schema versioning and storage-unavailable safety, and cookie helpers with Secure/SameSite=Lax/365-day expiry.

Purpose: Establish the persistence layer that every UI component in Phase 2+ will depend on, with correctness verified by automated tests.
Output: package.json, vitest.config.js, src/storage.js, src/cookies.js, and their corresponding test files — all tests green.
</objective>

<execution_context>
@C:/Users/gorus/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gorus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test infrastructure and localStorage adapter with TDD</name>
  <files>package.json, vitest.config.js, src/storage.js, tests/storage.test.js</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md
    .planning/phases/01-foundation/01-VALIDATION.md
  </read_first>
  <behavior>
    - saveMonth('2025-01', { 1: 'in-office', 2: 'at-home' }) writes to localStorage key 'attendance-2025-01' with schemaVersion:1
    - loadMonth('2025-01') returns { schemaVersion: 1, days: { 1: 'in-office', 2: 'at-home' } }
    - loadMonth for nonexistent key returns null
    - loadMonth for corrupt JSON (e.g., '{bad') returns null, does not throw
    - loadMonth for payload missing schemaVersion returns null
    - saveMonth returns false when localStorage.setItem throws (quota exceeded simulation)
    - loadMonth returns null when localStorage is unavailable
    - clearMonth('2025-01') removes the key from localStorage
    - saveMonth zero-pads month: saveMonth('2025-1', ...) should reject or normalize — key format is always 'attendance-YYYY-MM' with 2-digit month
  </behavior>
  <action>
    Step 1: Create package.json with devDependencies only:
    ```json
    {
      "name": "hybrid-attendance-tracker",
      "version": "1.0.0",
      "private": true,
      "type": "module",
      "scripts": {
        "test": "vitest run",
        "test:watch": "vitest",
        "test:coverage": "vitest run --coverage"
      },
      "devDependencies": {
        "vitest": "^3.0.0"
      }
    }
    ```
    CRITICAL: No "dependencies" field — only "devDependencies". This enforces NFR-1 (zero shipped dependencies).
    CRITICAL: "type": "module" enables ES module imports in .js files.

    Step 2: Create vitest.config.js:
    ```javascript
    import { defineConfig } from 'vitest/config';
    export default defineConfig({
      test: {
        environment: 'jsdom',
        include: ['tests/**/*.test.js'],
      },
    });
    ```

    Step 3: Run `npm install` to install vitest.

    Step 4 (RED): Create tests/storage.test.js with all tests from the <behavior> block above. Tests import from '../src/storage.js'. Use `describe`/`it` blocks. Mock localStorage unavailability by temporarily overriding window.localStorage with a Proxy that throws on setItem. Run `npx vitest run tests/storage.test.js` — all tests MUST FAIL (module doesn't exist yet).

    Step 5 (GREEN): Create src/storage.js implementing:
    - SCHEMA_VERSION = 1
    - STATUS constant object: { IN_OFFICE: 'in-office', AT_HOME: 'at-home', TIME_OFF: 'time-off', WFA: 'wfa', UNSET: 'unset' }
    - storageAvailable() function — MDN pattern: try setItem/removeItem on test key '__storage_test__', return true; catch returns false (with QuotaExceededError + length > 0 special case)
    - _available = storageAvailable('localStorage') cached at module load
    - saveMonth(yearMonth, days) — validates yearMonth matches /^\d{4}-\d{2}$/, returns false if _available is false, wraps setItem in try/catch, writes JSON.stringify({ schemaVersion: SCHEMA_VERSION, days }), returns true on success, false on error
    - loadMonth(yearMonth) — returns null if _available is false, wraps getItem+JSON.parse in try/catch, returns null if raw is null/undefined, returns null if parsed has no schemaVersion, returns parsed payload on success, returns null on any error
    - clearMonth(yearMonth) — wraps removeItem in try/catch
    - Export: saveMonth, loadMonth, clearMonth, STATUS, SCHEMA_VERSION

    Step 6: Run `npx vitest run tests/storage.test.js` — all tests MUST PASS.

    Step 7 (REFACTOR): Review for clarity. Ensure no `new Date(string)` calls exist. Ensure every localStorage call is inside try/catch.
  </action>
  <verify>
    <automated>npx vitest run tests/storage.test.js</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "devDependencies" with "vitest"
    - package.json does NOT contain a "dependencies" field (grep -c '"dependencies"' package.json returns 0)
    - package.json contains "type": "module"
    - vitest.config.js contains "environment: 'jsdom'"
    - src/storage.js contains "export function saveMonth("
    - src/storage.js contains "export function loadMonth("
    - src/storage.js contains "export function clearMonth("
    - src/storage.js contains "schemaVersion"
    - src/storage.js contains "SCHEMA_VERSION = 1"
    - src/storage.js contains "storageAvailable"
    - src/storage.js does NOT contain "new Date(" followed by a quote character (grep "new Date(['\"]" returns 0 matches)
    - Every localStorage.setItem call in src/storage.js is inside a try block
    - Every localStorage.getItem call in src/storage.js is inside a try block
    - tests/storage.test.js contains at least 7 it() or test() blocks
    - `npx vitest run tests/storage.test.js` exits with code 0
  </acceptance_criteria>
  <done>
    Vitest installed and configured. src/storage.js exports saveMonth, loadMonth, clearMonth, STATUS, SCHEMA_VERSION. All storage operations are wrapped in try/catch and return false/null on failure. schemaVersion:1 is included in every write. All tests in tests/storage.test.js pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cookie helpers with TDD</name>
  <files>src/cookies.js, tests/cookies.test.js</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md
    src/storage.js
  </read_first>
  <behavior>
    - setCookie('weekends-enabled', '1', { 'max-age': 31536000, 'SameSite': 'Lax', 'Secure': true }) writes to document.cookie a string containing "weekends-enabled=1", "max-age=31536000", "SameSite=Lax", "Secure"
    - getCookie('weekends-enabled') returns '1' after the setCookie call above
    - getCookie('nonexistent') returns undefined
    - setCookie URL-encodes name and value (setCookie('a=b', 'c;d') encodes the special characters)
    - deleteCookie('weekends-enabled') causes getCookie('weekends-enabled') to return undefined
    - setCookie defaults path to '/' when no path attribute provided
    - All cookie operations wrapped in try/catch — setCookie/getCookie/deleteCookie never throw
  </behavior>
  <action>
    Step 1 (RED): Create tests/cookies.test.js with all tests from the <behavior> block. Tests import { setCookie, getCookie, deleteCookie } from '../src/cookies.js'. Note: jsdom provides a basic document.cookie implementation. Before each test, clear document.cookie. Run `npx vitest run tests/cookies.test.js` — all tests MUST FAIL.

    Step 2 (GREEN): Create src/cookies.js implementing:
    - setCookie(name, value, attributes = {}) — defaults path to '/', builds cookie string as `encodeURIComponent(name) + '=' + encodeURIComponent(value)`, appends each attribute as `; key` or `; key=value` (if value !== true), sets document.cookie. Entire function body in try/catch, silent on error.
    - getCookie(name) — escapes regex special chars in name, matches against document.cookie using RegExp `(?:^|; )NAME=([^;]*)`, returns decodeURIComponent(match[1]) or undefined. Entire function body in try/catch, returns undefined on error.
    - deleteCookie(name) — calls setCookie(name, '', { 'max-age': 0, path: '/' }). This expires the cookie immediately.
    - Export: setCookie, getCookie, deleteCookie

    Step 3: Run `npx vitest run tests/cookies.test.js` — all tests MUST PASS.

    Step 4: Run full suite `npx vitest run` — ALL tests (storage + cookies) must pass.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - src/cookies.js contains "export function setCookie("
    - src/cookies.js contains "export function getCookie("
    - src/cookies.js contains "export function deleteCookie("
    - src/cookies.js contains "encodeURIComponent"
    - src/cookies.js contains "decodeURIComponent"
    - src/cookies.js contains "try" (at least one try/catch block)
    - src/cookies.js contains "path" and "'/' " (default path)
    - tests/cookies.test.js contains at least 5 it() or test() blocks
    - `npx vitest run` exits with code 0 (all test files pass)
  </acceptance_criteria>
  <done>
    src/cookies.js exports setCookie, getCookie, deleteCookie. setCookie defaults path to '/', builds attribute string with Secure/SameSite/max-age support. getCookie uses regex matching with URL decoding. deleteCookie sets max-age=0. All operations wrapped in try/catch. All tests pass alongside storage tests.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` passes all tests in both storage.test.js and cookies.test.js
- `grep -c '"dependencies"' package.json` returns 0 (no shipped dependencies — NFR-1)
- `grep -r "new Date(['\"]" src/ --include="*.js"` returns 0 matches (no string date construction — NFR-4 preemptive)
- Every public function in src/storage.js and src/cookies.js has corresponding test coverage
</verification>

<success_criteria>
- Vitest test infrastructure is installed and configured with jsdom environment
- src/storage.js provides saveMonth/loadMonth/clearMonth with schemaVersion:1 in every write
- All storage operations return false/null on failure — never throw
- src/cookies.js provides setCookie/getCookie/deleteCookie with proper attribute handling
- All cookie operations wrapped in try/catch — never throw
- Full test suite passes: `npx vitest run` exits 0
- No "dependencies" in package.json (NFR-1 enforced)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
